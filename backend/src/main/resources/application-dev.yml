spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/college_service
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

rag:
  enabled: true
  embedding:
    api-url: "http://localhost:8081/v1/embeddings"
